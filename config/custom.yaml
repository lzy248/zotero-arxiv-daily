zotero:
  user_id: ${oc.env:ZOTERO_ID}
  api_key: ${oc.env:ZOTERO_KEY}
  include_path: null # Or set a list of glob patterns, e.g. ["2026/survey/**","2026/reading-group/**"]

email:
  notes_collapsible: true
  sender: ${oc.env:SENDER}
  receiver: ${oc.env:RECEIVER}
  smtp_server: smtp.qq.com
  smtp_port: 465
  sender_password: ${oc.env:SENDER_PASSWORD}

llm:
  enabled: false
  reading_notes:
    enabled: true # Runs automatically after selection when llm.enabled is true.
  api:
    key: ${oc.env:OPENAI_API_KEY}
    base_url: ${oc.env:OPENAI_API_BASE}
  api_mode: chat_completion
  generation_kwargs:
    model: gpt-4o-mini

source:
  arxiv:
    fetch_full_text: false # Abstracts suffice for ranking and optional TLDR.
    category: [cs.AI, cs.LG, cs.CL, cs.CV, cs.IR, cs.NE, cs.MA, cs.RO, cs.SE, cs.DB, cs.DS, cs.DC, cs.CR]

executor:
  reranker: bm25
  max_paper_num: 3
  debug: ${oc.env:DEBUG,null}
  source: ['arxiv']

recommendation:
  enabled: true
  openalex:
    fields: [17] # Computer Science only; no Biology, Physics or Medicine rotation.
